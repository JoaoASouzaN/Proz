Elabore um algoritmo para que o usuário, através da entrada de dados, informe os seus dados pessoais. Alguns desses dados
fornecidos pelo usuário precisam ser apresentados na tela quando o algoritmo for executado, são eles:
 
- Nome;
- Endereço;
- Cidade;
- CPF;
- RG.

Início
    // Declaração das variáveis
    variável nome, endereço, cidade, cpf, rg: texto

    // Entrada de dados
    exibir "Digite seu nome: "
    ler nome

    exibir "Digite seu endereço: "
    ler endereço

    exibir "Digite sua cidade: "
    ler cidade

    exibir "Digite seu CPF: "
    ler cpf

    exibir "Digite seu RG: "
    ler rg

    // Exibição dos dados fornecidos
    exibir "Dados pessoais:"
    exibir "Nome: " + nome
    exibir "Endereço: " + endereço
    exibir "Cidade: " + cidade
    exibir "CPF: " + cpf
    exibir "RG: " + rg